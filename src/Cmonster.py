class monster:
    def __init__(self, name, dmg, init, currentHP, maxHP, currentMP, maxMP, giveXP, rarity):
        self.name = name
        self.dmg = dmg
        self.init = init
        self.level = 1
        self.currentHP = currentHP
        self.maxHP = maxHP
        self.currentMP = currentMP
        self.maxMP = maxMP
        self.giveXP = giveXP
        self.rarity =rarity


